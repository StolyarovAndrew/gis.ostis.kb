/* SCs_code0.1.0.Level 3 */

отдельные строения=> разбиение по признаку* : выдающиеся строения;;
отдельные строения=> разбиение по признаку* : невыдающиеся строения;;
отдельные строения∈ реальный объект;;
отдельные строения∈ подгруппа объектов местности;;
отдельные строения⊂ населенные пункты;
