/* SCs_code0.1.0.Level 3 */

переправы => разбиение по признаку* : паромы;;
переправы => разбиение по признаку* : перевозы;;
переправы => разбиение по признаку* : броды;;

переправы ∈ реальный объект;;
переправы ∈ группа объектов местности;;
переправы ⊂ переправы и морские пути;;
